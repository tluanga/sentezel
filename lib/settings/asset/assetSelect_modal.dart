import 'package:flutter/material.dart';

class AssetSelectModal extends StatelessWidget {
  const AssetSelectModal({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}
