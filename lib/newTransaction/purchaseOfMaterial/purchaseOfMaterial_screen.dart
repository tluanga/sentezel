import 'package:flutter/material.dart';

class PurchaseOfMaterialScreen extends StatelessWidget {
  const PurchaseOfMaterialScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}
